#include "MYCODES.h"


class WIENER{
public:

  // creates filter from source s and reference d (such as noise)
  vector<Double_t> create_filter(vector<Double_t> s, vector<Double_t> d){
    Int_t n = s.size();
    vector<Double_t> w;
  }
  
};
